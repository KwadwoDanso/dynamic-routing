// AdminPage.jsx — protected page (only reachable when authed)
export default function AdminPage() {
    return (
        <section>
            <h1>Admin Dashboard</h1>
            <p>Welcome to the Admin Dashboard.</p>
        </section>
    );
}